# Local use

Run only against a copy of an authorized customer artifact.

```bash
cd buyer-pack
PYTHONPATH=tools python3 -c 'from ai_assurance_tools import passport, provenance, recovery, evidence, inference; print("toolkit import ok")'
```

The included examples are synthetic format demonstrations, not customer proof.
